= render :partial => 'shared/header'

div.container.featured-products.content
  h2 Featured Products
  = render 'shared/show_products', collection: @products || 'No products'

= render :partial => 'shared/footer'